.env
.env.local
/data/
/IEA_data_*/
/IEA_data_*.tar.gz*
*.db
*.db-wal
*.db-shm
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
node_modules/
dist/
build/
.DS_Store
*.pem
*.log
pwa/.env.production
/tools/
backend/data/
*.tar.gz*
/orderflow_2*/

# Design canvas working files. The .dc.html artboard sources and canvas.json are
# the editable source and are kept; the seeded payload (~2.5MB, the editor bundled
# with the content) is regenerated from them on every change, so it is not.
.design/**/*.html
!.design/**/*.dc.html
