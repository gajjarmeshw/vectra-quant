version: '3.8'

services:
  vectra_quant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vectra_quant_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    restart: unless-stopped
